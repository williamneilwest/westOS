services:
  westos-frontend:
    build: ./frontend
    container_name: westos-frontend
    ports:
      - "5173:5173"
    networks:
      - westos

networks:
  westos:
    external: true
    name: westOS
